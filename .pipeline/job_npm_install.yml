parameters:
  path: ''

jobs:
- job: NpmInstall
  displayName: 'npm install'
  dependsOn: CheckChanges
  steps:
  - bash: |
      cd "${{ parameters.path }}"
      npm install
    name: npm_install
    displayName: 'Install NPM dependencies'
